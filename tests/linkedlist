
class punkt {
	int eintrag;
	punkt next;
}

void add(punkt p, int n) {
	while(p.next)
		p = p.next;
	punkt neu = new punkt;
	neu.eintrag = n;
	neu.next = null;
	p.next = neu;
}

void print(punkt p) {
	while(p.next) {
		p = p.next;
		p.eintrag;
	}
}

void main() {
	punkt list = new punkt;
	list.next = null;
	for (int i = -10; i != 10; i++;)
		add(list, i);
	print(list);
}
