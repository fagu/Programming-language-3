int main() {
	int max = 10;
	int[][] a = new int[][max];
	for (int i = 0; i < max; i++;) {
		a[i] = new int[i+1];
		for (int k = 0; k < i+1; k++;)
			a[i][k] = i+k;
	}
	for (int i = 0; i < max; i++;)
		for (int k = 0; k < i+1; k++;)
			print_int(a[i][k]);
}
